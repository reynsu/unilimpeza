/** Hero-image art-direction breakpoint — keep in sync with CSS @media rules. */
export const HERO_MOBILE_MAX_PX = 720;
export const HERO_MOBILE_MEDIA = `(max-width: ${HERO_MOBILE_MAX_PX}px)`;
export const HERO_DESKTOP_MEDIA = `(min-width: ${HERO_MOBILE_MAX_PX + 1}px)`;
